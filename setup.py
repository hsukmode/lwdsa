from setuptools import setup

setup(name='lwdsa',
      version='0.1',
      description="a 'light weight' libary that implements base data structures and algorithms",
      url='https://github.com/hsukmode/lwdsa',
      author='hsukmode',
      license='MIT',
      packages=['lwdsa'],
      zip_safe=False)
